(define (problem task)

(:domain robocluedo)

(:objects
	;; hypothesis IDs
	ID1 ID2 ID3 ID4 ID5 ID6 - hypothesis-ID
)

(:init
	;; the system must be initalized
	; (not (kb-init ))
	
	;; by default max loop is 3
	(= (max-loop ) 3)
	
	
)

(:goal (and
;; fluents:    (= (battery-amount r1) 100) or other comparisons
;; predicates: (on-site building site1)
))

)
